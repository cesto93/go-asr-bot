services:
  bot:
    platform: linux/arm64
    image: ghcr.io/cesto93/go-asr-bot:arm64-latest
    network_mode: host
    env_file:
      - .env
    volumes:
      - .:/opt/go-asr-bot:z
      - asr-models:/opt/go-asr-bot/models
    restart: unless-stopped

volumes:
  asr-models:
